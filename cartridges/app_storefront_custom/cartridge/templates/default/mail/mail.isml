<iscontent type="text/html" charset="UTF-8" />

<html>

<body>

    ${Resource.msg('mail.text', 'mail', null)}
    <br />
    <img src="${pdict.image}">
    <br />
    ${Resource.msg('product.name', 'mail', null)}
    <a href="${URLUtils.url('Product-Show', 'pid', pdict.product.pid)}">
        <isprint value="${pdict.product.name}" />
    </a>
    <br />
    ${Resource.msg('product.description', 'mail', null)} <isprint value="${pdict.product.shortDescription}">
        <br />
        ${Resource.msg('product.price', 'mail', null)}
        <isprint value="${pdict.product.priceModel.price.currencyCode}" />
        <isprint value="${pdict.product.priceModel.price}" />
        <br />
        ${Resource.msg('product.qty', 'mail', null)} <isprint value="${pdict.product.stepQuantity}">

</body>

</html>